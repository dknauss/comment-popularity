---
phase: 08-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inc/class-comment-popularity.php
  - inc/class-visitor.php
  - inc/widgets/experts/class-widget-experts.php
  - uninstall.php
  - tests/test-bootstrap-init.php
  - tests/test-comment-popularity.php
  - tests/test-visitor.php
  - tests/test-uninstall.php
  - tests/test-helpers.php
  - tests/test-wilson-ranking.php
  - ROADMAP.md
  - CHANGELOG_UNRELEASED.md
  - README.md
  - README.txt
  - .planning/prs/README.md
autonomous: true
must_haves:
  truths:
    - "Wilson ranking remains implemented fork behavior and is not reintroduced as new feature work in this phase"
    - "All behavior changes in Phase 8 follow a Red-Green-Refactor cycle with failing tests written first"
    - "Author karma updates only follow registered comment user_id values, not email fallback"
    - "Plugin uninstall removes blog-scoped and user-scoped state on single-site and multisite installs"
    - "Public vote callback behavior is covered by automated tests"
    - "No Phase 8 task is complete until touched runtime behavior, tests, and planning docs all agree"
    - "Planning docs describe the fork's actual execution state rather than an outdated upstream queue"
  artifacts:
    - path: "inc/class-comment-popularity.php"
      provides: "Vote processing with user_id-only author karma attribution and stable callback handling"
      contains: "get_comment_author_id"
    - path: "inc/class-visitor.php"
      provides: "Visitor storage and validation rules aligned to the chosen server-side policy"
      contains: "hmn_cp_interval"
    - path: "tests/test-comment-popularity.php"
      provides: "AJAX callback and vote transition regression coverage"
      contains: "comment_vote_callback"
    - path: "tests/test-visitor.php"
      provides: "Guest and member persistence regression coverage"
      contains: "HMN_CP_Visitor_Guest"
    - path: "ROADMAP.md"
      provides: "Canonical remediation backlog and Wilson status"
      contains: "Phase 8"
  key_links:
    - from: "inc/class-comment-popularity.php"
      to: "tests/test-comment-popularity.php"
      via: "Callback and vote transition invariants are exercised directly"
      pattern: "comment_vote_callback|comment_vote"
    - from: "inc/class-visitor.php"
      to: "tests/test-visitor.php"
      via: "Guest/member persistence and validation behavior are covered"
      pattern: "retrieve_logged_votes|save_logged_votes|is_vote_valid"
    - from: "ROADMAP.md"
      to: ".planning/prs/README.md"
      via: "Fork-first roadmap supersedes optional upstream export scaffolding"
      pattern: "fork-first|upstream export"
---

<objective>
Address the remaining correctness, lifecycle, coverage, and planning contradictions identified in the fork review.

Purpose: The fork already ships Wilson ranking, modern CI, and a PHP 8.2 baseline, but several live defects and documentation inconsistencies remain. This plan prioritizes the remaining issues in the order required to make the fork internally coherent: fix author identity, make uninstall multisite-safe, resolve the vote-interval contradiction, close public-surface test gaps, land the outstanding experts widget fixes, and then reconcile the planning/export documents.

Wilson status: Wilson ranking is already implemented in the fork. This plan preserves that behavior and focuses on the unresolved surrounding invariants, coverage gaps, and documentation drift.

Output: A coherent fork baseline where vote consequences, lifecycle cleanup, public callback behavior, widget behavior, tests, and planning documents all agree.
</objective>

<delivery_standard>
This phase executes under the companion TDD and coverage standards in:

- `/Users/danknauss/Documents/GitHub/Comment System Planning/TDD_WPVIP_TESTING.md`
- `/Users/danknauss/Documents/GitHub/Comment System Planning/TEST_COVERAGE_MATRIX.md`

Required loop for every task:
1. RED: write or extend a failing regression test that demonstrates the current defect or missing contract.
2. GREEN: implement the smallest runtime change needed to make the new test pass.
3. REFACTOR: simplify the touched code only after the new tests are green.
4. SYNC: update roadmap/changelog/planning text in the same slice so docs do not lag the code.
5. VERIFY: rerun the targeted tests first, then the phase verification commands.

Current verified coverage gaps that Phase 8 must close:
- `comment_vote_callback()` still has zero execution in the current coverage report.
- `tests/test-visitor.php` currently covers member persistence only; guest single-site and multisite paths remain untested.
- `tests/test-uninstall.php` covers single-site cleanup only; blog-scoped multisite cleanup is untested.
- `HMN_CP_Widget_Experts::get_experts()` and `get_gravatar_url()` still have zero execution.

Secondary partial-coverage work that should be evaluated after the blocking defects are fixed:
- direct sorting assertions for both `karma` and `wilson` ranking modes,
- explicit negative-capability regressions for vote permissions,
- upgrade/backfill behavior for legacy comments and Wilson metadata,
- widget/template output contracts once the widget helpers are stabilized.
</delivery_standard>

<execution_context>
@/Users/danknauss/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ROADMAP.md
@CHANGELOG_UNRELEASED.md
@.planning/prs/README.md
@comment-popularity.php
@inc/class-comment-popularity.php
@inc/class-visitor.php
@inc/widgets/experts/class-widget-experts.php
@uninstall.php
@tests/test-comment-popularity.php
@tests/test-visitor.php
@tests/test-uninstall.php
@tests/test-helpers.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix author-karma identity so only registered comment user_id values drive karma changes</name>
  <files>inc/class-comment-popularity.php, tests/test-comment-popularity.php, tests/test-helpers.php, CHANGELOG_UNRELEASED.md</files>
  <action>
RED:
- Add a failing regression in `tests/test-comment-popularity.php` proving a guest comment that reuses a registered user's email does not mutate that user's karma.
- Keep the existing happy-path assertion for a registered comment with `user_id` set.

GREEN:
- Remove the email fallback from `get_comment_author_id()` so comment vote processing only updates author karma when `comment->user_id` is present.

REFACTOR + SYNC:
- Simplify any now-dead fallback logic.
- Update helper tests and changelog text so they describe the same `user_id`-only runtime behavior.
  </action>
  <verify>
- `get_comment_author_id()` returns `0` for guest comments even when comment author email matches an existing user.
- Vote processing still updates karma for comments authored by registered users.
- Helper tests and vote-path tests agree on `user_id`-only author identification.
  </verify>
  <done>Author karma attribution is consistent across runtime, helpers, tests, roadmap, and changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Make uninstall fully multisite-safe for blog-scoped plugin state</name>
  <files>uninstall.php, tests/test-uninstall.php, ROADMAP.md</files>
  <action>
RED:
- Add failing tests in `tests/test-uninstall.php` for multisite cleanup of `comment_popularity_prefs` and `hmn_cp_guests_logged_votes`.
- Preserve the existing single-site uninstall assertions as a non-regression baseline.

GREEN:
- Update uninstall cleanup to remove blog-scoped options that the runtime stores with `get_blog_option()` / `update_blog_option()` on multisite, including `comment_popularity_prefs` and `hmn_cp_guests_logged_votes`.

REFACTOR + SYNC:
- Keep capability teardown and comment cleanup intact.
- Document the resolved lifecycle behavior in the roadmap once the multisite tests pass.
  </action>
  <verify>
- No plugin prefs or guest vote state remain after uninstall on single-site.
- No plugin prefs or guest vote state remain after uninstall on multisite for the active blog.
- Existing uninstall tests continue to pass.
  </verify>
  <done>Runtime storage model and uninstall cleanup are aligned for both single-site and multisite.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the vote-interval contradiction by choosing one server-side policy</name>
  <files>inc/class-visitor.php, inc/class-comment-popularity.php, tests/test-comment-popularity.php, tests/test-wilson-ranking.php, ROADMAP.md, CHANGELOG_UNRELEASED.md</files>
  <action>
Choose one of these paths and apply it consistently:

1. Preferred: retire `hmn_cp_interval` entirely if duplicate-vote rejection plus explicit transition rules are the intended server-side policy.
2. Alternative: implement real interval enforcement for both member and guest visitors if throttling is still a required behavior.

RED:
- Add or update failing tests to represent the chosen policy before touching runtime code.
- If retiring the interval, replace the current flood-oriented assertions with duplicate-vote/state-transition assertions.
- If keeping the interval, add deterministic tests for member and guest throttling without using sleeps.

GREEN:
- Remove dead code, dead tests, and stale references if retiring the interval, or add the missing enforcement if the interval remains.

REFACTOR + SYNC:
- Do not leave the current mixed state where an interval property exists, tests configure it, but the runtime never uses it.
- Update roadmap and changelog language so they describe only the chosen server-side rule.
  </action>
  <verify>
- The live code, tests, roadmap, and changelog describe the same vote policy.
- Repeated identical votes remain rejected.
- Vote transition behavior still keeps legacy weight and Wilson metadata aligned.
  </verify>
  <done>The server-side voting policy is singular, documented, and fully test-backed.</done>
</task>

<task type="auto">
  <name>Task 4: Add guest persistence and public AJAX callback regression coverage</name>
  <files>tests/test-comment-popularity.php, tests/test-visitor.php, tests/test-bootstrap-init.php</files>
  <action>
RED:
- Add failing guest persistence tests for single-site and multisite behavior in `tests/test-visitor.php`.
- Add failing callback-level tests in `tests/test-comment-popularity.php` or a dedicated callback test file.

GREEN:
- Expand the suite to cover the public surfaces that are still effectively untested:
  1. Guest visitor persistence on single-site and multisite.
  2. `comment_vote_callback()` success and failure paths:
   - invalid nonce
   - invalid comment ID
   - invalid vote action
   - missing visitor
   - guest voting disabled
   - successful upvote/downvote/undo response
  3. Any bootstrap setup required to exercise the callback path cleanly.

REFACTOR:
- Prefer direct callback-level tests over indirect assertions through `comment_vote()`.
- Keep bootstrap setup isolated so callback tests remain deterministic and do not depend on global suite ordering.
  </action>
  <verify>
- Coverage report shows non-zero execution for `comment_vote_callback()`.
- `tests/test-visitor.php` contains guest-path coverage in addition to member-path coverage.
- PHPUnit passes in both single-site and multisite mode.
  </verify>
  <done>The public vote endpoint and guest storage model are covered by automated regression tests.</done>
</task>

<task type="auto">
  <name>Task 5: Land the remaining experts widget fixes and cover them</name>
  <files>inc/widgets/experts/class-widget-experts.php, tests/, ROADMAP.md, CHANGELOG_UNRELEASED.md</files>
  <action>
RED:
- Add focused failing coverage for empty expert result sets and avatar URL generation.

GREEN:
- Fix the two outstanding widget defects:
  1. Initialize `$return = array();` in `get_experts()` before iterating.
  2. Switch Gravatar URLs from `http://` to `https://`.

REFACTOR + SYNC:
- Cover any widget helper path touched while fixing the file.
- Update roadmap/changelog statements so they stop treating these fixes as both pending and already-landed at the same time.
  </action>
  <verify>
- `get_experts()` returns an empty array when no experts exist.
- No `http://gravatar.com` reference remains in runtime code.
- Coverage report shows execution for the widget helper path you changed.
  </verify>
  <done>Experts widget runtime behavior, coverage, and planning text are aligned.</done>
</task>

<task type="auto">
  <name>Task 6: Reconcile roadmap, GSD, export scaffolding, and release metadata with actual fork state</name>
  <files>ROADMAP.md, .planning/prs/README.md, README.md, README.txt, CHANGELOG_UNRELEASED.md</files>
  <action>
Make the documentation layer authoritative:

1. Keep `ROADMAP.md` as the canonical fork execution document.
2. Add the outcomes of Tasks 1–5 to the roadmap in current-state terms.
3. Reword `.planning/prs/README.md` as optional historical export scaffolding unless there is an active upstream export decision.
4. Ensure README and readme.txt metadata match the fork runtime and CI baseline.
5. Preserve Wilson as current fork behavior while making clear that remaining Wilson-related work is stabilization and coverage, not feature introduction.

TDD note:
- This task is documentation-only, so the RED/GREEN split is replaced by a proof step: point each changed statement at the live code or a passing test before editing it.

Do not leave mixed language that says the repo is fork-first while also presenting the old upstream branch stack as current execution truth.
  </action>
  <verify>
- `ROADMAP.md` does not describe stale branch/PR state as canonical current workflow.
- Wilson is described consistently as implemented fork behavior.
- README, readme.txt, roadmap, and changelog do not contradict the code.
  </verify>
  <done>The planning and release docs reflect the actual fork state instead of mixed historical queue state.</done>
</task>

</tasks>

<verification>
- `composer lint`
- `composer analyse:phpstan`
- `composer analyse:psalm`
- `WP_VERSION=6.4 composer test:setup && composer test:integration`
- `WP_VERSION=6.4 composer test:setup && WP_MULTISITE=1 composer test:integration`
- `composer test:coverage`

Additional review checkpoints:
- `tests/cache/coverage/clover.xml` shows non-zero execution for `comment_vote_callback()`.
- No repo-owned planning or docs file contradicts the live Wilson status or PHP 8.2 floor.
</verification>

<success_criteria>
- Author karma updates only track registered comment `user_id` values.
- Uninstall cleanup is complete on both single-site and multisite.
- Vote interval behavior is either implemented or retired, not implied-but-dead.
- Public callback and guest persistence behavior are covered by tests.
- Experts widget defects are fixed and covered.
- Wilson remains implemented fork behavior.
- The roadmap and GSD docs are current execution truth for the fork.
</success_criteria>

<output>
After completion, create `.planning/phases/08-remediation/08-01-SUMMARY.md`
</output>
