---
phase: 08-remediation
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - tests/test-comment-popularity.php
  - tests/test-visitor.php
  - tests/test-uninstall.php
  - tests/test-bootstrap-init.php
  - tests/test-admin.php
  - tests/test-wilson-ranking.php
  - inc/class-comment-popularity.php
  - inc/class-visitor.php
  - inc/widgets/experts/class-widget-experts.php
  - uninstall.php
  - ROADMAP.md
autonomous: true
must_haves:
  truths:
    - "Phase 8 remediation follows Red-Green-Refactor and does not ship deferred tests"
    - "Security-critical and core-voting domains must not remain Gap items when this phase closes"
    - "Coverage priorities follow runtime risk: callback validation, persistence, uninstall, author karma, widget helpers, then secondary admin/upgrade behavior"
    - "Wilson ranking stays implemented fork behavior throughout this plan"
  artifacts:
    - path: "tests/test-comment-popularity.php"
      provides: "Direct callback-level vote contract coverage"
      contains: "comment_vote_callback"
    - path: "tests/test-visitor.php"
      provides: "Guest and member persistence coverage"
      contains: "HMN_CP_Visitor_Guest"
    - path: "tests/test-uninstall.php"
      provides: "Single-site and multisite lifecycle cleanup coverage"
      contains: "run_uninstall"
    - path: "tests/test-wilson-ranking.php"
      provides: "Wilson metadata and ranking-mode regression coverage"
      contains: "ranking_mode"
    - path: "ROADMAP.md"
      provides: "Current-state test backlog and completion criteria"
      contains: "Phase 8"
---

<objective>
Turn the current test and coverage state into an ordered TDD execution matrix for Phase 8 remediation.

Purpose: The repository now has a working PHPUnit, coverage, PHPStan, and PHPCS baseline, but the remaining gaps are uneven. This plan converts the live state of the suite into a risk-ordered backlog so the fork can close the highest-value blind spots first and avoid “covered enough” guesswork.

Output: A coverage-driven execution order that matches the current codebase, current tests, and the companion TDD standard.
</objective>

<execution_context>
@/Users/danknauss/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ROADMAP.md
@tests/test-comment-popularity.php
@tests/test-visitor.php
@tests/test-uninstall.php
@tests/test-bootstrap-init.php
@tests/test-admin.php
@tests/test-wilson-ranking.php
@tests/cache/coverage/clover.xml
@/Users/danknauss/Documents/GitHub/Comment System Planning/TDD_WPVIP_TESTING.md
@/Users/danknauss/Documents/GitHub/Comment System Planning/TEST_COVERAGE_MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the blocking coverage backlog to the current runtime risks</name>
  <files>ROADMAP.md, tests/cache/coverage/clover.xml</files>
  <action>
Use the live suite and coverage report as the source of truth for Phase 8 priorities:

Gap:
1. `comment_vote_callback()` validation and response contract.
2. Guest persistence on single-site and multisite.
3. Multisite uninstall cleanup for blog-scoped state.
4. Experts widget helper coverage (`get_experts()`, `get_gravatar_url()`).

Partial:
1. Vote transition matrix assertions and duplicate-vote policy wording.
2. Ranking-mode sorting behavior under both `karma` and `wilson`.
3. Negative-capability regression coverage for voting.
4. Admin settings persistence/sanitization.

Track these in current-state terms, not as historical queue notes.
  </action>
  <verify>
- The roadmap and plan files describe the same blocking and secondary coverage items.
- No Phase 8 work item claims coverage that the current clover report does not show.
  </verify>
  <done>Coverage priorities reflect the real suite and the real runtime risks.</done>
</task>

<task type="auto">
  <name>Task 2: Treat every correctness fix as test-first and slice-specific</name>
  <files>tests/test-comment-popularity.php, tests/test-visitor.php, tests/test-uninstall.php, tests/test-wilson-ranking.php</files>
  <action>
For each Phase 8 code change:
1. Start with a narrowly-scoped failing regression test.
2. Make the minimum runtime change to pass that test.
3. Add edge assertions for stored meta, response payloads, and cleanup side effects in the same slice.
4. Do not batch unrelated behavior changes into one red/green cycle.

Recommended slice order:
1. author-karma identity
2. multisite uninstall
3. vote-interval decision
4. callback and guest persistence
5. experts widget helpers
  </action>
  <verify>
- Each implemented fix can point to the failing test that introduced it.
- No behavior change lands without same-slice regression coverage.
  </verify>
  <done>Phase 8 execution is test-led rather than patch-led.</done>
</task>

<task type="auto">
  <name>Task 3: Define completion for the secondary coverage backlog</name>
  <files>tests/test-comment-popularity.php, tests/test-admin.php, tests/test-wilson-ranking.php, ROADMAP.md</files>
  <action>
After the blocking defects are fixed, close the remaining partial areas in this order:
1. add explicit sorting assertions for both ranking modes,
2. add negative-capability regressions for voting paths,
3. tighten admin settings persistence and sanitization tests,
4. decide whether upgrade/backfill behavior requires dedicated tests in this phase or an explicit defer.

Do not treat these as blockers ahead of callback, persistence, uninstall, or author-karma correctness.
  </action>
  <verify>
- Core voting, lifecycle, and public callback domains have no Gap items before secondary work begins.
- Any deferred secondary item is explicitly recorded in the roadmap with a reason.
  </verify>
  <done>The remaining partial domains are either closed or intentionally deferred with explicit rationale.</done>
</task>

</tasks>

<verification>
- `composer lint`
- `composer analyse:phpstan`
- `composer analyse:psalm`
- `WP_VERSION=6.4 composer test:setup && composer test:integration`
- `WP_VERSION=6.4 composer test:setup && WP_MULTISITE=1 composer test:integration`
- `composer test:coverage`

Coverage checkpoints:
- `comment_vote_callback()` has non-zero execution.
- `HMN_CP_Widget_Experts::get_experts()` has non-zero execution.
- `HMN_CP_Widget_Experts::get_gravatar_url()` has non-zero execution.
- No Phase 8 roadmap item claims a domain is covered when clover still marks it as untouched.
</verification>

<success_criteria>
- The Phase 8 backlog is explicitly ordered by runtime risk and live coverage gaps.
- TDD sequencing is mandatory for every behavior change.
- The roadmap, coverage plan, and test suite describe the same current state.
</success_criteria>

<output>
After completion, create `.planning/phases/08-remediation/08-02-SUMMARY.md`
</output>
