---
phase: 07-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inc/widgets/experts/class-widget-experts.php
  - composer.json
  - comment-popularity.php
autonomous: true
must_haves:
  truths:
    - "Gravatar images in the experts widget load over HTTPS without mixed-content warnings"
    - "get_experts() returns an empty array when no expert users exist, not an undefined variable"
    - "composer.json declares php ^8.2 as a requirement"
  artifacts:
    - path: "inc/widgets/experts/class-widget-experts.php"
      provides: "Experts widget with HTTPS gravatar and safe array init"
      contains: "https://gravatar.com"
    - path: "composer.json"
      provides: "PHP 8.2 minimum constraint"
      contains: "\"php\": \"^8.2\""
  key_links:
    - from: "composer.json"
      to: "comment-popularity.php"
      via: "PHP version check at bootstrap"
      pattern: "PHP_VERSION|version_compare|8\\.2"
---

<objective>
Fix the two remaining experts widget bugs and declare PHP 8.2 as the project minimum.

Purpose: Establish the PHP 8.2 baseline that the PSR-4 and PHPStan plans depend on, and close out the last known widget bugs that aren't addressed on any existing branch.

Output: Patched experts widget, updated composer.json with PHP constraint, bootstrap version guard.
</objective>

<execution_context>
@/Users/danknauss/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@ROADMAP.md
@inc/widgets/experts/class-widget-experts.php
@composer.json
@comment-popularity.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix experts widget bugs (HTTPS Gravatar + array initialization)</name>
  <files>inc/widgets/experts/class-widget-experts.php</files>
  <action>
Two changes in this file:

1. In `get_gravatar_url()` (line 232), change the return URL from `'http://gravatar.com/avatar/' . $hash` to `'https://gravatar.com/avatar/' . $hash`.

2. In `get_experts()` (line 190), add `$return = array();` immediately after the `$experts = get_users( $args );` line (before the foreach loop on line 210). This prevents an undefined variable notice when `$experts` is empty and the foreach never executes.
  </action>
  <verify>
- `grep -n 'https://gravatar.com' inc/widgets/experts/class-widget-experts.php` returns the updated line.
- `grep -n '$return = array()' inc/widgets/experts/class-widget-experts.php` returns the initialization line.
- No `http://gravatar.com` remains in the file.
  </verify>
  <done>Gravatar URL uses HTTPS; $return is initialized before population; no undefined variable risk when no experts exist.</done>
</task>

<task type="auto">
  <name>Task 2: Declare PHP 8.2 minimum in composer.json and add bootstrap guard</name>
  <files>composer.json, comment-popularity.php</files>
  <action>
1. In `composer.json`, add `"php": "^8.2"` to the `"require"` section (alongside the existing `"twig/twig": "2.*"` entry).

2. In `comment-popularity.php`, add an early version guard near the top of the file (after the plugin header comment, before any class loading):

```php
if ( version_compare( PHP_VERSION, '8.2', '<' ) ) {
    add_action( 'admin_notices', function () {
        printf(
            '<div class="notice notice-error"><p>%s</p></div>',
            esc_html__( 'Comment Popularity requires PHP 8.2 or higher. Please upgrade your PHP version.', 'comment-popularity' )
        );
    } );
    return;
}
```

Do NOT change `vendor-dir`, `autoload`, `require-dev`, or any other existing composer.json settings. Those will be updated in later plans.

Note: The existing `codex/ci-quality` branch CI tests PHP 7.4/8.1. The CI matrix update to 8.2+ will happen in Plan 07-02 alongside the PHPStan addition, since both modify the same workflow file.
  </action>
  <verify>
- `php -r "echo json_decode(file_get_contents('composer.json'))->require->php;"` outputs `^8.2`.
- `grep 'version_compare.*8.2' comment-popularity.php` finds the guard.
- `composer validate` passes without errors.
  </verify>
  <done>composer.json requires PHP ^8.2; bootstrap exits gracefully with admin notice on older PHP; no other composer.json settings disturbed.</done>
</task>

</tasks>

<verification>
- `composer validate` passes.
- `grep -r 'http://gravatar' inc/` returns no results (only https).
- The plugin file has an early return guard for PHP < 8.2.
- No changes to files outside the three listed.
</verification>

<success_criteria>
- Experts widget Gravatar URLs are HTTPS.
- get_experts() initializes its return array.
- PHP 8.2 minimum is enforced at both Composer and runtime levels.
</success_criteria>

<output>
After completion, create `.planning/phases/07-modernization/07-01-SUMMARY.md`
</output>
